# Portfolio Guy Stals

Personal portfolio website showcasing Guy Stals' background, projects, technical skills, certificates, and contact information.

## Overview

This is a lightweight static website built with:

- HTML
- CSS
- JavaScript

The site includes:

- An about section
- Professional project highlights
- An academic project reference
- Skills and certificates
- Contact information and contact form

## Project Structure

```text
.
|-- index.html
|-- styles.css
|-- script.js
|-- guy-photo.jpg
```

## Running Locally

Because this is a static website, you can open `index.html` directly in your browser.

If you prefer using VS Code with Live Server:

1. Open the project folder in VS Code.
2. Right-click `index.html`.
3. Choose `Open with Live Server`.

## Publishing to GitHub

1. Create a new repository on GitHub.
2. Initialize Git locally if needed:

```bash
git init
git add .
git commit -m "Initial commit"
```

3. Connect your remote repository and push:

```bash
git remote add origin <your-repository-url>
git branch -M main
git push -u origin main
```

## Author

Guy Stals
